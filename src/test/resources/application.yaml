spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:spring-boot-tests-${random.uuid};DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: sa
  application:
    name: spring-boot-tests
    jackson:
      property-naming-strategy: SNAKE_CASE
